export class Song {
  id = 0;
  name = '';
  albumId = 0; // This would represent the ID of the album the song belongs to
  length = 0; // Length of the song in seconds, for example
  // You can add more properties as needed
}
